# نظام معرض المشاريع الأكاديمي (Academic Exhibition System)

لوحة تحكم ويب لإدارة **الطلاب** و**الفرق** و**الفعاليات** و**النقاط** مع واجهة حديثة، دعم **العربية/الإنجليزية**، وثيم **فاتح/داكن**، وحفظ تلقائي للإعدادات.

> رابط الموقع بعد النشر على GitHub Pages سيكون بالشكل:
> `https://<USERNAME>.github.io/<REPO>/` (أو `https://<USERNAME>.github.io/` لو كان اسم المستودع `<USERNAME>.github.io`).

## ✨ المميزات
- دعم **اللغة العربية والإنجليزية** + حفظ الإعدادات تلقائيًا.
- **ثيم فاتح/داكن** + التحكم بحجم الخط.
- إدارة:
  - الطلاب (إضافة/تعديل/حذف + منع التكرار)
  - الفرق (أعضاء الفريق + النقاط)
  - الفعاليات (تسجيل مشاركين + منع التكرار + إزالة مشارك)
- **Toast Notifications** لرسائل النجاح/الخطأ بشكل أنيق.
- **Backup / Restore**: تصدير واستيراد البيانات بصيغة JSON.
- **Responsive**: سايدبار متجاوب للموبايل مع Overlay.

## 🧱 التقنيات
- Vite + React
- تخزين محلي LocalStorage

## 🚀 تشغيل محليًا
```bash
npm install
npm run dev
```

## 🏗️ Build
```bash
npm run build
npm run preview
```

## 🌐 النشر على GitHub Pages (جاهز)
المستودع يحتوي Workflow جاهز داخل:
`.github/workflows/deploy.yml`

- ادفع الكود إلى GitHub على الفرع `main`.
- من **Settings → Pages** اختر **Source: GitHub Actions**.

> مهم: Vite يحتاج ضبط `base` حسب نوع رابط Pages. هنا تم ضبطه تلقائيًا عبر متغير البيئة `VITE_BASE` أثناء الـ build.

## 📝 License
استخدمه وطور عليه بحرية.

---

# English (Short)
A modern dashboard to manage students, teams, events, and points with **Arabic/English**, **light/dark theme**, autosave settings, and JSON backup/restore.

### Run
```bash
npm install
npm run dev
```

### Deploy
GitHub Actions workflow included. Enable **Settings → Pages → Source: GitHub Actions**.
